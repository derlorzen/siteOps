# Copy to /etc/siteops-browser-runner.env and chmod 600.
# The installer generates a token automatically when this file does not exist.
HOST=127.0.0.1
PORT=3200
BROWSER_RUNNER_TOKEN=replace-with-a-long-random-token
